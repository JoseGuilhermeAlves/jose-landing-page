# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.metadata

# Deploy artifacts — builds publicados na branch gh-pages, nunca no source.
# (Codex commitava estes por engano; o deploy real vai pra gh-pages.)
.deploy-temp/
.deploy-temp2/
.deploy-web/
.gh-pages/

# Workspace mode: only the root pubspec.lock is versioned
**/pubspec.lock
!/pubspec.lock

# FVM
.fvm/
!.fvmrc

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Screenshots / scratch refs na raiz (debris de design, nunca versionar).
# Assets reais do app vivem em apps/*/assets e packages/*/assets.
/*.png
/*.PNG

# Docs internos de planejamento/notas — NUNCA publicos (repo e open source).
# Ficam no disco local, fora do versionamento. README.md e
# PHOTO_CREDITS.md continuam versionados (publicos).
/CLAUDE.md
/PROJECT.md
/DESIGN.md
/COPY_REWRITE_PROPOSAL.md
/Veredito-analise-fable-5.md
/instrucoes-custom-painter.md
/licoes-custom-painter-boss-1a1.md

# Pasta de referencias de design (GIFs/PNGs de boss, fontes do CV, etc.) —
# material de trabalho interno, fora do repo publico.
/cosmic-boss-reference-theme/
