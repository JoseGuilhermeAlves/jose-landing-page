/// Feature de contato — secao de fechamento da landing: funil `mailto:`
/// primario, CTAs alternativos (GitHub · LinkedIn · WhatsApp · currículo
/// PDF). Quem decide como abrir cada URI e o app shell (via `onOpenUri`).
library;

export 'src/presentation/widgets/contact_section.dart';
