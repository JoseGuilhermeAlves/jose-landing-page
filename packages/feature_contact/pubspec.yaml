name: feature_contact
description: Secao de contato — funil mailto, CTAs alternativos e download do curriculo.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  design_system:
    path: ../design_system
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
