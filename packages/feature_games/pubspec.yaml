name: feature_games
description: Raycaster 3D cyberpunk e experiencias interativas (rota /games, deferred).
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  design_system:
    path: ../design_system
  flutter:
    sdk: flutter
  go_router: ^16.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
