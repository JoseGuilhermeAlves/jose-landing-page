name: feature_labs
description: Playground tecnico com Custom Painters interativos (rota /labs, deferred).
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  animations:
    path: ../animations
  core:
    path: ../core
  design_system:
    path: ../design_system
  flutter:
    sdk: flutter
  go_router: ^16.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
